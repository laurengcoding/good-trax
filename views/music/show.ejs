<%- include('../partials/header') %>


<section id="show-page">
    <div>
        <h2>Artist: </h2>
        <p><%= music.artist %></p>
    </div>
    <div>
        <h2>Album: </h2>
        <p><%= music.album %></p>
    </div>
    <div>
        <h2>Release Date: </h2>
        <p><%= music.releaseDate.month %> / <%= music.releaseDate.year %></p>
    </div>
    <div>
        <img src="<%= music.image %>" alt="Album Artwork">
    </div>
    
    <button>Add Review</button>
   
    <button>Add To List</button>

</section>
<% if (user) { %>
<section id="add-review-section">
    <h1>Reviews</h1>
    <form id="add-review-form" method="POST" action="/music/<%= music._id %>/reviews">
        <label>Review:</label>
        <textarea name="content"></textarea>
        <label>Rating:</label>
        <select name="rating">
            <option value="1">💿</option>
            <option value="2">💿💿</option>
            <option value="3">💿💿💿</option>
            <option value="4">💿💿💿💿</option>
            <option value="5" selected>💿💿💿💿💿</option>
        </select>
        <input type="submit" id="Add Review">
    </form>
</section>
<% }; %>

<!-- <% if (music.reviews.length) { %> -->
 <% music.reviews.forEach(function (r) { %>
<section id="show-review-section">
    <h2>User</h2>
    <div class="review-user">
        <img src="<%= r.userAvatar %>" referrerpolicy="no-referrer" alt="avatar">
        <p><%= r.userName %></p>
    </div>
    <h2>Review</h2>
    <p><%= r.content %></p>
    <h2>Rating</h2>
    <p><%= r.rating %></p>
</section>
<% }); %>
<!-- <% }; %> -->



<%- include('../partials/footer') %>
